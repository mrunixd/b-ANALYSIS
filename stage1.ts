import { getData, setData } from "./dataStore";

function register(email: string, password: string): number {
    return 999;
}

function login(email: string, password: string): number {
    return 999;
}

function storeInfo(
    salary: number,
    rent: number,
    vehicle: number,
    food: number,
    memberships: number,
    insurance: number,
    debt: number
) {
    return 999;
}